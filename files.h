#include <iostream>
#include <string>
#include <fstream>

using namespace std;

string getMode();

int getTimeAndIncrement(string mode, double *time, double *increment);

int writeTimes(double white, double black);
